vaadin.launch-browser=true
spring.application.name=syntora
vaadin.debug=true
server.port=${PORT:5000}

# PostgreSQL Datasource (Configured for Neon / Cloud & Local)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-dry-paper-axfn36wp.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_uzHm9KMPer6s}
spring.datasource.driver-class-name=org.postgresql.Driver

# H2 In-Memory Database Configuration (PostgreSQL Mode) (Commented out)
# spring.datasource.url=jdbc:h2:mem:society_management;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=YEAR,MONTH
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.h2.console.settings.web-allow-others=true

# SQL Initialization
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
server.servlet.session.timeout=2h
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
# application.properties
server.error.include-message=always
server.error.include-stacktrace=always

#logging.level.org.springframework=DEBUG
#logging.level.org.springframework.security=DEBUG
#logging.level.org.springframework.web=DEBUG
#server.error.whitelabel.enabled=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.mode=HTML
spring.thymeleaf.suffix=.html
# Resend API (replaces SMTP — uses HTTPS so works on Render)
# Set RESEND_API_KEY environment variable on Render dashboard
resend.api.key=${RESEND_API_KEY:}
mail.from=${MAIL_FROM:societyhub18@gmail.com}
mail.from.name=${MAIL_FROM_NAME:Syntora Society Management}


#logging.config=classpath:log4j2.xml
#spring.main.banner-mode=off
#logging.level.root=off
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.logging.Log4j2AutoConfiguration

logging.level.root=DEBUG
debug=true

spring.messages.basename=i18n/messages
spring.messages.encoding=UTF-8
spring.mvc.locale-resolver=cookie
spring.mvc.locale= en
spring.thymeleaf.cache=false









